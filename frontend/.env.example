# Local development — leave empty to route /api through Vite proxy (recommended)
VITE_API_URL=

# Production (Vercel) — replace with your Render backend URL
# VITE_API_URL=https://your-app.onrender.com
